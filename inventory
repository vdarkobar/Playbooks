[all:vars]

# Username for ssh connection
ansible_user=<username>                                     # change

# Run commands as root user?
ansible_become=yes

# How do I become root user? Use sudo.
ansible_become_method=sudo

# Python interpreter version
ansible_python_interpreter='/usr/bin/env python3'

# Password for sudo
#ansible_become_pass='<pass>'                               # try without; uncomment, change 

[servers]
<ip1>                                                       # change/add other servers

# Aliases
test ansible_host=<ip2>                                     # change/add other alias(es)=server(s)
